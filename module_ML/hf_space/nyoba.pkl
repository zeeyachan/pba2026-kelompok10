import gradio as gr
import joblib

model = joblib.load("sentiment_model.pkl")

def predict(text):
    return model.predict([text])[0]

demo = gr.Interface(
    fn=predict,
    inputs="text",
    outputs="text",
    title="Sentiment Analysis Tokopedia"
)

demo.launch()
